// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/lbrictson/TinyMonitor/ent/alertchannel"
	"github.com/lbrictson/TinyMonitor/ent/monitor"
	"github.com/lbrictson/TinyMonitor/ent/predicate"
)

// AlertChannelQuery is the builder for querying AlertChannel entities.
type AlertChannelQuery struct {
	config
	limit        *int
	offset       *int
	unique       *bool
	order        []OrderFunc
	fields       []string
	predicates   []predicate.AlertChannel
	withMonitors *MonitorQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AlertChannelQuery builder.
func (acq *AlertChannelQuery) Where(ps ...predicate.AlertChannel) *AlertChannelQuery {
	acq.predicates = append(acq.predicates, ps...)
	return acq
}

// Limit adds a limit step to the query.
func (acq *AlertChannelQuery) Limit(limit int) *AlertChannelQuery {
	acq.limit = &limit
	return acq
}

// Offset adds an offset step to the query.
func (acq *AlertChannelQuery) Offset(offset int) *AlertChannelQuery {
	acq.offset = &offset
	return acq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (acq *AlertChannelQuery) Unique(unique bool) *AlertChannelQuery {
	acq.unique = &unique
	return acq
}

// Order adds an order step to the query.
func (acq *AlertChannelQuery) Order(o ...OrderFunc) *AlertChannelQuery {
	acq.order = append(acq.order, o...)
	return acq
}

// QueryMonitors chains the current query on the "monitors" edge.
func (acq *AlertChannelQuery) QueryMonitors() *MonitorQuery {
	query := &MonitorQuery{config: acq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := acq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := acq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(alertchannel.Table, alertchannel.FieldID, selector),
			sqlgraph.To(monitor.Table, monitor.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, alertchannel.MonitorsTable, alertchannel.MonitorsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(acq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AlertChannel entity from the query.
// Returns a *NotFoundError when no AlertChannel was found.
func (acq *AlertChannelQuery) First(ctx context.Context) (*AlertChannel, error) {
	nodes, err := acq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{alertchannel.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (acq *AlertChannelQuery) FirstX(ctx context.Context) *AlertChannel {
	node, err := acq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AlertChannel ID from the query.
// Returns a *NotFoundError when no AlertChannel ID was found.
func (acq *AlertChannelQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = acq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{alertchannel.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (acq *AlertChannelQuery) FirstIDX(ctx context.Context) string {
	id, err := acq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AlertChannel entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AlertChannel entity is found.
// Returns a *NotFoundError when no AlertChannel entities are found.
func (acq *AlertChannelQuery) Only(ctx context.Context) (*AlertChannel, error) {
	nodes, err := acq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{alertchannel.Label}
	default:
		return nil, &NotSingularError{alertchannel.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (acq *AlertChannelQuery) OnlyX(ctx context.Context) *AlertChannel {
	node, err := acq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AlertChannel ID in the query.
// Returns a *NotSingularError when more than one AlertChannel ID is found.
// Returns a *NotFoundError when no entities are found.
func (acq *AlertChannelQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = acq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{alertchannel.Label}
	default:
		err = &NotSingularError{alertchannel.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (acq *AlertChannelQuery) OnlyIDX(ctx context.Context) string {
	id, err := acq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AlertChannels.
func (acq *AlertChannelQuery) All(ctx context.Context) ([]*AlertChannel, error) {
	if err := acq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return acq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (acq *AlertChannelQuery) AllX(ctx context.Context) []*AlertChannel {
	nodes, err := acq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AlertChannel IDs.
func (acq *AlertChannelQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := acq.Select(alertchannel.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (acq *AlertChannelQuery) IDsX(ctx context.Context) []string {
	ids, err := acq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (acq *AlertChannelQuery) Count(ctx context.Context) (int, error) {
	if err := acq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return acq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (acq *AlertChannelQuery) CountX(ctx context.Context) int {
	count, err := acq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (acq *AlertChannelQuery) Exist(ctx context.Context) (bool, error) {
	if err := acq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return acq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (acq *AlertChannelQuery) ExistX(ctx context.Context) bool {
	exist, err := acq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AlertChannelQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (acq *AlertChannelQuery) Clone() *AlertChannelQuery {
	if acq == nil {
		return nil
	}
	return &AlertChannelQuery{
		config:       acq.config,
		limit:        acq.limit,
		offset:       acq.offset,
		order:        append([]OrderFunc{}, acq.order...),
		predicates:   append([]predicate.AlertChannel{}, acq.predicates...),
		withMonitors: acq.withMonitors.Clone(),
		// clone intermediate query.
		sql:    acq.sql.Clone(),
		path:   acq.path,
		unique: acq.unique,
	}
}

// WithMonitors tells the query-builder to eager-load the nodes that are connected to
// the "monitors" edge. The optional arguments are used to configure the query builder of the edge.
func (acq *AlertChannelQuery) WithMonitors(opts ...func(*MonitorQuery)) *AlertChannelQuery {
	query := &MonitorQuery{config: acq.config}
	for _, opt := range opts {
		opt(query)
	}
	acq.withMonitors = query
	return acq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AlertChannelType string `json:"alert_channel_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AlertChannel.Query().
//		GroupBy(alertchannel.FieldAlertChannelType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (acq *AlertChannelQuery) GroupBy(field string, fields ...string) *AlertChannelGroupBy {
	grbuild := &AlertChannelGroupBy{config: acq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := acq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return acq.sqlQuery(ctx), nil
	}
	grbuild.label = alertchannel.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AlertChannelType string `json:"alert_channel_type,omitempty"`
//	}
//
//	client.AlertChannel.Query().
//		Select(alertchannel.FieldAlertChannelType).
//		Scan(ctx, &v)
func (acq *AlertChannelQuery) Select(fields ...string) *AlertChannelSelect {
	acq.fields = append(acq.fields, fields...)
	selbuild := &AlertChannelSelect{AlertChannelQuery: acq}
	selbuild.label = alertchannel.Label
	selbuild.flds, selbuild.scan = &acq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a AlertChannelSelect configured with the given aggregations.
func (acq *AlertChannelQuery) Aggregate(fns ...AggregateFunc) *AlertChannelSelect {
	return acq.Select().Aggregate(fns...)
}

func (acq *AlertChannelQuery) prepareQuery(ctx context.Context) error {
	for _, f := range acq.fields {
		if !alertchannel.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if acq.path != nil {
		prev, err := acq.path(ctx)
		if err != nil {
			return err
		}
		acq.sql = prev
	}
	return nil
}

func (acq *AlertChannelQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AlertChannel, error) {
	var (
		nodes       = []*AlertChannel{}
		_spec       = acq.querySpec()
		loadedTypes = [1]bool{
			acq.withMonitors != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AlertChannel).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AlertChannel{config: acq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, acq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := acq.withMonitors; query != nil {
		if err := acq.loadMonitors(ctx, query, nodes,
			func(n *AlertChannel) { n.Edges.Monitors = []*Monitor{} },
			func(n *AlertChannel, e *Monitor) { n.Edges.Monitors = append(n.Edges.Monitors, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (acq *AlertChannelQuery) loadMonitors(ctx context.Context, query *MonitorQuery, nodes []*AlertChannel, init func(*AlertChannel), assign func(*AlertChannel, *Monitor)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[string]*AlertChannel)
	nids := make(map[string]map[*AlertChannel]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(alertchannel.MonitorsTable)
		s.Join(joinT).On(s.C(monitor.FieldID), joinT.C(alertchannel.MonitorsPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(alertchannel.MonitorsPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(alertchannel.MonitorsPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	neighbors, err := query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
		assign := spec.Assign
		values := spec.ScanValues
		spec.ScanValues = func(columns []string) ([]any, error) {
			values, err := values(columns[1:])
			if err != nil {
				return nil, err
			}
			return append([]any{new(sql.NullString)}, values...), nil
		}
		spec.Assign = func(columns []string, values []any) error {
			outValue := values[0].(*sql.NullString).String
			inValue := values[1].(*sql.NullString).String
			if nids[inValue] == nil {
				nids[inValue] = map[*AlertChannel]struct{}{byID[outValue]: {}}
				return assign(columns[1:], values[1:])
			}
			nids[inValue][byID[outValue]] = struct{}{}
			return nil
		}
	})
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "monitors" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (acq *AlertChannelQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := acq.querySpec()
	_spec.Node.Columns = acq.fields
	if len(acq.fields) > 0 {
		_spec.Unique = acq.unique != nil && *acq.unique
	}
	return sqlgraph.CountNodes(ctx, acq.driver, _spec)
}

func (acq *AlertChannelQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := acq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (acq *AlertChannelQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   alertchannel.Table,
			Columns: alertchannel.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: alertchannel.FieldID,
			},
		},
		From:   acq.sql,
		Unique: true,
	}
	if unique := acq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := acq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, alertchannel.FieldID)
		for i := range fields {
			if fields[i] != alertchannel.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := acq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := acq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := acq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := acq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (acq *AlertChannelQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(acq.driver.Dialect())
	t1 := builder.Table(alertchannel.Table)
	columns := acq.fields
	if len(columns) == 0 {
		columns = alertchannel.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if acq.sql != nil {
		selector = acq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if acq.unique != nil && *acq.unique {
		selector.Distinct()
	}
	for _, p := range acq.predicates {
		p(selector)
	}
	for _, p := range acq.order {
		p(selector)
	}
	if offset := acq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := acq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AlertChannelGroupBy is the group-by builder for AlertChannel entities.
type AlertChannelGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (acgb *AlertChannelGroupBy) Aggregate(fns ...AggregateFunc) *AlertChannelGroupBy {
	acgb.fns = append(acgb.fns, fns...)
	return acgb
}

// Scan applies the group-by query and scans the result into the given value.
func (acgb *AlertChannelGroupBy) Scan(ctx context.Context, v any) error {
	query, err := acgb.path(ctx)
	if err != nil {
		return err
	}
	acgb.sql = query
	return acgb.sqlScan(ctx, v)
}

func (acgb *AlertChannelGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range acgb.fields {
		if !alertchannel.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := acgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (acgb *AlertChannelGroupBy) sqlQuery() *sql.Selector {
	selector := acgb.sql.Select()
	aggregation := make([]string, 0, len(acgb.fns))
	for _, fn := range acgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(acgb.fields)+len(acgb.fns))
		for _, f := range acgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(acgb.fields...)...)
}

// AlertChannelSelect is the builder for selecting fields of AlertChannel entities.
type AlertChannelSelect struct {
	*AlertChannelQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (acs *AlertChannelSelect) Aggregate(fns ...AggregateFunc) *AlertChannelSelect {
	acs.fns = append(acs.fns, fns...)
	return acs
}

// Scan applies the selector query and scans the result into the given value.
func (acs *AlertChannelSelect) Scan(ctx context.Context, v any) error {
	if err := acs.prepareQuery(ctx); err != nil {
		return err
	}
	acs.sql = acs.AlertChannelQuery.sqlQuery(ctx)
	return acs.sqlScan(ctx, v)
}

func (acs *AlertChannelSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(acs.fns))
	for _, fn := range acs.fns {
		aggregation = append(aggregation, fn(acs.sql))
	}
	switch n := len(*acs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		acs.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		acs.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := acs.sql.Query()
	if err := acs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
